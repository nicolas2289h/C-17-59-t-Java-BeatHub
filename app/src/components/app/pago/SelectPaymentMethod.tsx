import { Dispatch, SetStateAction } from "react";

export const SelectPaymentMethod = ({
  setStep,
}: {
  setStep: Dispatch<SetStateAction<number>>;
}) => {
  return <section className="w-3/5">SelectPaymentMethod</section>;
};
